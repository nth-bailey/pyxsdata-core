## 📒 Description

<!-- Provide a brief description of your PR and what problem it solves. -->

Resolves #

## 🔗 Changes Made

<!-- Outline the key changes introduced in this PR. -->

- 

## 🛫 Checklist

- [ ] Code is formatted with `cargo fmt --check` (or `cargo fmt`).
- [ ] Code passes linter checks (`cargo clippy --all-targets -- -D warnings`).
- [ ] Rust unit tests pass (`cargo test`).
- [ ] Python integration tests pass (`pytest`).
- [ ] Benchmarks have been run (`pytest --benchmark-only`) if this change affects performance.
- [ ] Commits follow [Conventional Commits](https://www.conventionalcommits.org/).
